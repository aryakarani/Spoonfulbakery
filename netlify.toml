[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18.17.0"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Next.js SSR is handled by the Netlify Next.js Runtime v5 via the plugin.
# No manual SPA catch-all redirect is needed.